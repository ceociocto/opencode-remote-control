{
  "name": "opencode-remote-control",
  "version": "0.1.0.0",
  "description": "Remote control for OpenCode via Telegram and Feishu",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.2.27",
    "grammy": "^1.30.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
